<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="equipo.tres.lexi">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Lexi">
        <activity android:name=".ui.ejercicios.CompletarActivity"></activity>
        <activity android:name=".ui.ejercicios.OpcionesActivity" />
        <activity android:name=".ui.ejercicios.MainActivity" />
        <activity android:name=".ui.ejercicios.TraducirActivity" />
        <activity android:name=".ui.home.HomeActivity" />
        <activity android:name=".ui.cursos.LeccionAvanzadaActivity" />
        <activity android:name=".ui.cursos.TemasActivity" />
        <activity android:name=".ui.home.NivelesActivity" />
        <activity android:name=".ui.home.IntroductionActivity" />
        <activity android:name=".ui.cursos.MisCursosActivity" />
        <activity android:name=".ui.perfil.PerfilActivity" />
        <activity android:name=".ui.login.RegistroActivity" />
        <activity
            android:name=".ui.home.MenuActivity"
            android:label="@string/title_activity_menu" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>